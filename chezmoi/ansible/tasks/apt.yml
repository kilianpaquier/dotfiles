# Code generated by AI (Claude Opus 5)

- name: Apt full upgrade
  ansible.builtin.apt:
    cache_valid_time: 3600
    update_cache: true
    upgrade: full

- name: Base packages
  ansible.builtin.apt:
    name:
      - bash-completion
      - bubblewrap
      - build-essential
      - ca-certificates
      - curl
      - file
      - gettext-base
      - git
      - gnupg
      - jq
      - make
      - man
      - procps
      - python3
      - ripgrep
      - socat
      - tree
      - unzip
      - vim
      - wget
      - yq
      - "{{ shell }}"
