# Code generated by AI (Claude Opus 5)

- name: Podman data
  ansible.builtin.command:
    cmd: podman unshare rm -rf {{ ansible.home }}/.local/share/containers
    removes: "{{ ansible.home }}/.local/share/containers"
  become: false

- name: Podman packages
  ansible.builtin.apt:
    name: [podman, podman-compose]
    state: absent
    purge: true
